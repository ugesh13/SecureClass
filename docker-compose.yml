version: "3.9"

services:
  db:
    image: postgres:16
    environment:
      POSTGRES_USER: secureclass
      POSTGRES_PASSWORD: secureclass
      POSTGRES_DB: secureclass
    ports: ["5432:5432"]
    volumes: ["pgdata:/var/lib/postgresql/data"]

volumes:
  pgdata:
